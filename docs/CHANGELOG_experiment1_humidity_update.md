# Experiment 1 humidity split update

This starter bundle was updated after a code-level clarification that Experiment 1 uses:

- train humidities: `np.linspace(0, 1, 30)`
- test humidity: `np.arange(0.1, 1.0, 0.05)`

The file `benchmark/splits/humidity_split_matched.json` was updated accordingly.
